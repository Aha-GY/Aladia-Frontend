export default defineNuxtPlugin(() => {
  //   const originalWarn = console.warn;
  //   console.warn = function (...args) {
  //     if (typeof args[0] === 'string') {
  //       if (args[0].startsWith('quill')) return;
  //       if (args[0].includes('-ms-high-constrast')) return;
  //       if (args[0].includes('Hydration node mismatch')) return;
  //       if (args[0].includes('Added non-passive event listener')) return;
  //     }
  //     originalWarn.apply(console, args);
  //   };
});
